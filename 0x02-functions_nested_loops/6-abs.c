#include "main.h"

/**
 * _abs - Computes the absolute value of an interger
 * @t: integerto be computed
 * Return: the absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
